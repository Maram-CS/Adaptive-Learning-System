<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Courses</title>
<link rel="stylesheet" href="/Style/courses.css">


<div class="page-container">

  <!-- SIDEBAR -->
  <div class="sidebar">
    <div class="logo">
      <img src="/img/logoBleu.png" class="logo-img">
      <span>My Courses</span>
    </div>
    <div class="menu">
      <ul>
        <li class="active"><a href="/App/courses">All Courses</a></li>
        <li><a href="/App/studentDashboard">Dashboard</a></li>
        <li><a href="/App/favoriteCourses">Favorite Courses</a></li>
        <li><a href="/App/infoProfile">My Profile</a></li>
      </ul>
    </div>
    <a href="/App/home"><button class="logout">Sign out</button></a>
  </div>

  <!-- MAIN -->
  <div class="main">

    <!-- Header -->
    <div class="main-header">
      <h2>Courses <span>Dashboard</span></h2>
      <div class="header-actions">
        <input type="text" id="searchInput" class="search-bar" placeholder="Search courses...">
        <div class="buttons">
          <button class="icon-btn">🔔</button>
          <button class="icon-btn">💬</button>
          <a href="/App/infoProfile"><button class="icon-btn">👤</button></a>
        </div>
      </div>
    </div>

    <!-- Filter Buttons -->
    <div class="filter-buttons">
      <button onclick="filterCourses('all', this)" class="active-filter">All</button>
      <button onclick="filterCourses('frontend', this)">Frontend</button>
      <button onclick="filterCourses('backend', this)">Backend</button>
      <button onclick="filterCourses('database', this)">Database</button>
      <button onclick="filterCourses('tools', this)">Tools</button>
      <button onclick="filterCourses('design', this)">Design</button>
      <button onclick="filterCourses('deployment', this)">Deployment</button>
    </div>

    <!-- Courses Grid -->
    <div class="courses-container" id="coursesContainer">

      <div class="item" data-category="frontend">
        <img src="/img/html.png" class="course-img">
        <h3>HTML & CSS</h3>
        <p>Learn the fundamentals of web development using HTML and CSS.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="html-css"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="frontend">
        <img src="/img/javascript.png" class="course-img">
        <h3>JavaScript Basics</h3>
        <p>Understand the core concepts of JavaScript programming.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="javascript"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="frontend">
        <img src="/img/react.png" class="course-img">
        <h3>React JS</h3>
        <p>Build dynamic user interfaces with React library.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="react"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="backend">
        <img src="/img/node.png" class="course-img">
        <h3>Node.js</h3>
        <p>Learn backend development using Node.js and Express.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="nodejs"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="backend">
        <img src="/img/python.png" class="course-img">
        <h3>Python Basics</h3>
        <p>Get started with Python programming language.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="python"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="backend">
        <img src="/img/Django.png" class="course-img">
        <h3>Django</h3>
        <p>Develop web applications using Django framework.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="django"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="backend">
        <img src="/img/PHP.png" class="course-img">
        <h3>PHP</h3>
        <p>Learn web development with the PHP programming language.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="php"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="tools">
        <img src="/img/GitHub.png" class="course-img">
        <h3>GitHub</h3>
        <p>Learn version control with GitHub.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="github"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="tools">
        <img src="/img/Git.png" class="course-img">
        <h3>Git</h3>
        <p>Learn version control with Git.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="git"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="design">
        <img src="/img/Figma.png" class="course-img">
        <h3>Figma</h3>
        <p>Learn UI/UX design with Figma.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="figma"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="database">
        <img src="/img/MySQL.png" class="course-img">
        <h3>MySQL</h3>
        <p>Learn SQL database management with MySQL.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="mysql"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="database">
        <img src="/img/MongoDB.png" class="course-img">
        <h3>MongoDB</h3>
        <p>Learn NoSQL database management with MongoDB.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="mongodb"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

      <div class="item" data-category="deployment">
        <img src="/img/Vercel.png" class="course-img">
        <h3>Vercel</h3>
        <p>Deploy and host web applications with Vercel.</p>
        <div class="card-divider"></div>
        <div class="star-rating" data-course="vercel"></div>
        <div class="rating-label">Tap to rate</div>
      </div>

    </div>
  </div>
</div>

<div class="footer">© 2026 Eduphin. All rights reserved.</div>

<script>

/* ── RATING TEXT ─────────────────────────────────── */
const ratingText = {
  0: 'Tap to rate',
  1: 'Poor',
  2: 'Fair',
  3: 'Good',
  4: 'Very Good',
  5: 'Excellent! 🎉'
};

/* ── BUILD STARS FOR ONE CARD ────────────────────── */
function buildStars(container) {
  const course   = container.getAttribute('data-course');
  const saved    = parseInt(localStorage.getItem('rating_' + course)) || 0;
  const label    = container.nextElementSibling; // .rating-label

  container.innerHTML = '';

  for (let i = 1; i <= 5; i++) {
    const star = document.createElement('span');
    star.classList.add('star');
    star.textContent = '★';
    star.dataset.value = i;
    if (i <= saved) star.classList.add('filled');

    /* Hover in */
    star.addEventListener('mouseenter', () => {
      const val = parseInt(star.dataset.value);
      highlightStars(container, val);
      label.textContent = ratingText[val];
      label.classList.remove('rated');
    });

    /* Hover out — restore saved */
    star.addEventListener('mouseleave', () => {
      const cur = parseInt(localStorage.getItem('rating_' + course)) || 0;
      highlightStars(container, cur);
      label.textContent = ratingText[cur];
      label.classList.toggle('rated', cur > 0);
    });

    /* Click — save */
    star.addEventListener('click', () => {
      const val = parseInt(star.dataset.value);
      localStorage.setItem('rating_' + course, val);
      highlightStars(container, val);
      label.textContent = ratingText[val];
      label.classList.add('rated');

      /* Bounce */
      star.style.transform = 'scale(1.6)';
      setTimeout(() => { star.style.transform = ''; }, 220);
    });

    container.appendChild(star);
  }

  /* Restore saved state */
  highlightStars(container, saved);
  label.textContent  = ratingText[saved];
  label.classList.toggle('rated', saved > 0);
}

function highlightStars(container, value) {
  container.querySelectorAll('.star').forEach(s => {
    s.classList.toggle('filled', parseInt(s.dataset.value) <= value);
  });
}

/* ── INIT ────────────────────────────────────────── */
document.querySelectorAll('.star-rating').forEach(buildStars);


/* ── SEARCH ──────────────────────────────────────── */
const searchInput = document.getElementById('searchInput');
const items = document.querySelectorAll('.item');

searchInput.addEventListener('keyup', function () {
  const filter = searchInput.value.toLowerCase();
  items.forEach(item => {
    item.style.display = item.textContent.toLowerCase().includes(filter) ? 'flex' : 'none';
  });
});


/* ── CATEGORY FILTER ─────────────────────────────── */
function filterCourses(category, btn) {
  document.querySelectorAll('.filter-buttons button').forEach(b => b.classList.remove('active-filter'));
  btn.classList.add('active-filter');
  items.forEach(item => {
    const cat = item.getAttribute('data-category');
    item.style.display = (category === 'all' || cat === category) ? 'flex' : 'none';
  });
}
</script>

</body>
</html>